rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY (emulador): permitir todo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
